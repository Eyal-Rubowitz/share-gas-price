export const metadata = {
    titl: 'New station',
    description: 'New station page to create new station with all its info'
}

export default function NewStationLayout({ children }) {

    return (
        <div>
            {children}
        </div>
    )
}